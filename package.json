{
  "scripts": {
    "build": "rm release.zip && zip -r release.zip . -x '*.git*'  -x '*node_modules*'"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "dotenv": "^8.2.0",
    "ejs": "^2.6.1",
    "telegraf": "^3.30.1",
    "telegraf-command-parts": "^1.0.3"
  }
}
